<div class="page-header">
  <h1><%= @user.full_name %></h1>
</div>

<% if @posts %>
  <% @posts.each do |posts| %>
    <div class="well">
      <%= post.content %>
      <hr />
      <%= link_to time_ago_in_words(post.created_at), post_path(status) %> ago
    </div>
  <% end %>
<% end %>